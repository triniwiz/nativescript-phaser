import { Phaser } from 'nativescript-phaser';
console.log(new Phaser().message);
import { Component } from "@angular/core";

@Component({
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
